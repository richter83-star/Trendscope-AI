# Backend server configuration
PORT=4000
# Comma-separated list of allowed origins for CORS (leave blank for all during local dev)
ALLOWED_ORIGINS=

# Watchlist limits per subscription tier
WATCHLIST_FREE_LIMIT=10
WATCHLIST_STARTER_LIMIT=50
WATCHLIST_PRO_LIMIT=500

# Number of mock products to generate for search responses
MOCK_PRODUCT_COUNT=40
