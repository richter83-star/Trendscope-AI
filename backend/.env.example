# API port configuration
PORT=4000

# Allowed origins for CORS (comma-separated). Leave empty to allow all.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006

# Watchlist limits per subscription tier
WATCHLIST_FREE_LIMIT=10
WATCHLIST_STARTER_LIMIT=50
WATCHLIST_PRO_LIMIT=500

# Number of mock products generated for demo responses
MOCK_PRODUCT_COUNT=40
